const db = require('../models/dbModels');

const apiController = {};

//retrive all the homeInfos from the user
apiController.allSavedforId, (req, res, next) => {};

//edit the home info
apiController.editHomeInfo, (req, res, next) => {};

//delete the home
apiController.deleteHome, (req, res, next) => {};

//save the home info
apiController.addtoDB, (req, res, next) => {};

module.exports = apiController;
