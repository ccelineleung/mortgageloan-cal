import React from 'react';

const Homepage = () => {
  return (
    <div>
      <button>Home</button>
      <button>History</button>
      <button>Account</button>
    </div>
  );
};

export default Homepage;
